clc 
clear all;

%%%%
RobotCon = ConnectRobot;
calcSurfacePoint = laserPointToRealPoint;
CollectLaserPoints = collectLaserPoint;
%%%%


t = RobotCon.connect();

Scanpoints = create_scan_points(200,200,100,5);

LaserPoints = CollectLaserPoints(Scanpoints);

surfacePoint = calcSurfacePoint.calcSurfacePoint(LaserPoints)


